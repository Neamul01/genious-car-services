import React from 'react';

const Footer = () => {
    return (
        <footer>
            <p>
                <small>Copywrite &copy;</small>{new Date().getFullYear()} Noman
            </p>
        </footer>
    );
};

export default Footer;